window.addEventListener('load',()=>{document.querySelector('.premium-loader')?.classList.add('hide')});
document.querySelector('.menu-toggle')?.addEventListener('click',()=>document.querySelector('.nav-links')?.classList.toggle('open'));
const revealObs=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')})},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>revealObs.observe(el));
document.querySelectorAll('[data-count]').forEach(el=>{let end=parseInt(el.dataset.count||el.textContent,10),cur=0,step=Math.max(1,Math.ceil(end/60));let run=()=>{cur+=step;if(cur>=end){el.textContent=end+'+';return}el.textContent=cur;requestAnimationFrame(run)};revealObs.observe(el);setTimeout(run,700)});
document.addEventListener('mousemove',e=>{let x=(e.clientX/window.innerWidth-.5)*18,y=(e.clientY/window.innerHeight-.5)*18;document.querySelectorAll('[data-parallax]').forEach((el,i)=>{el.style.transform=`translate3d(${x/(i+1)}px,${y/(i+1)}px,0)`})});
document.querySelectorAll('.filter-btn').forEach(btn=>btn.addEventListener('click',()=>{let f=btn.dataset.filter;document.querySelectorAll('.filter-btn').forEach(b=>b.classList.remove('active'));btn.classList.add('active');document.querySelectorAll('[data-category]').forEach(card=>{card.style.display=(f==='all'||card.dataset.category===f)?'block':'none'})}));
const lb=document.querySelector('.lightbox'),lbc=document.querySelector('.lightbox-content');document.querySelectorAll('[data-lightbox]').forEach(card=>card.addEventListener('click',()=>{let src=card.dataset.src,type=card.dataset.type||'image';if(!lb||!lbc||!src)return;lbc.innerHTML=type==='video'?`<video src="${src}" controls autoplay playsinline></video>`:`<img src="${src}" alt="Wedding Shedding Gallery">`;lb.classList.add('open')}));document.querySelector('.lightbox-close')?.addEventListener('click',()=>{lb.classList.remove('open');lbc.innerHTML=''});lb?.addEventListener('click',e=>{if(e.target===lb){lb.classList.remove('open');lbc.innerHTML=''}});
(function canvasScene(){const c=document.getElementById('weddingScene');if(!c)return;const ctx=c.getContext('2d');let W,H,items=[];function resize(){W=c.width=innerWidth;H=c.height=Math.max(innerHeight,650);items=Array.from({length:90},(_,i)=>({x:Math.random()*W,y:Math.random()*H,r:Math.random()*3+1,s:Math.random()*1+.25,a:Math.random()*Math.PI*2,type:i%3}))}function draw(){ctx.clearRect(0,0,W,H);items.forEach(p=>{p.y+=p.s;p.x+=Math.sin(p.a+=.012)*.45;if(p.y>H+20){p.y=-20;p.x=Math.random()*W}ctx.save();ctx.translate(p.x,p.y);ctx.rotate(p.a);if(p.type===0){ctx.fillStyle='rgba(247,214,217,.68)';ctx.beginPath();ctx.ellipse(0,0,p.r*3,p.r*1.4,0,0,Math.PI*2);ctx.fill()}else{ctx.fillStyle=p.type===1?'rgba(212,175,55,.65)':'rgba(255,255,255,.55)';ctx.beginPath();ctx.arc(0,0,p.r,0,Math.PI*2);ctx.fill()}ctx.restore()});requestAnimationFrame(draw)}resize();addEventListener('resize',resize);draw()})();
